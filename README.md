This project implements the K-means clustering algorithm from scratch in Python.
We do not use sklearn, scipy, or any built-in clustering function.
The program reads gene expression data, filters interested genes, normalizes the data,
runs K-means for different K values, and reports clustering results.
